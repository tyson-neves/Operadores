let a = 1;

a = a + 1; //binário infix (in de que o operador esta entre a"+"1 )
a += 1;
a ++; // operador unário.... (postfix pois o operador esta depois) 

// outras formas de escrever abaixo
++ a; //(prefix pois ela esta antes do operando)
a --;
-- a;

console.log(a);